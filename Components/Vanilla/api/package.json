{
  "name": "api",
  "private": true,
  "version": "0.0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "pack": "npm run build && npm pack --pack-destination ../Web-Components/dist/api"
  },
  "devDependencies": {
    "@types/node": "^20.8.7",
    "@vitest/coverage-istanbul": "^0.34.6",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vite-plugin-dts": "^3.6.0",
    "vitest": "^0.34.6"
  }
}
